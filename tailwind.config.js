export default {
  theme: {
    extend: {
      fontFamily: {
        circular: ['FlowCircular', 'sans-serif'],
      },
      fontFamily: {
        sans: ['Michroma', 'sans-serif'],
      },
    },
    plugins: [require('tailwind-scrollbar')],

  },
};
